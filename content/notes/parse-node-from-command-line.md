# Scary Node Command Line Parse

Did you know - you can run any sketchy Node JS code directly from the command line, and have it execute?

Seems dangerous.

```
node -e "console.log(require('chalk').red('bro'))"
```